import 'package:flutter/material.dart';

class PendingDuesPage extends StatelessWidget {
  const PendingDuesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Pending Dues Page'),
      ),
    );
  }
}
