import 'package:flutter/material.dart';

class PoolStatusPage extends StatelessWidget {
  const PoolStatusPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Pool Status Page'),
      ),
    );
  }
}
