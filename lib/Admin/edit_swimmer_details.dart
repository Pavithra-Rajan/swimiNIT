import 'package:flutter/material.dart';

class EditSwimmerPage extends StatelessWidget {
  const EditSwimmerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Edit Swimmer Page'),
    );
  }
}
